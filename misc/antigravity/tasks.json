{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Evaluate Jsonnet",
            "type": "shell",
            "command": "tk tool jpath \"${file}\" >/dev/null 2>&1 && tk eval \"${file}\" || jsonnet \"${file}\"",
            "presentation": {
                "reveal": "always",
                "panel": "shared"
            },
            "problemMatcher": []
        }
    ]
}